# Imagem

#### Atribuir nome e tag

```batch
docker tag <nome ou id do container> <nome>:<tag>
```
> ":\<tag>" não é obrigatório

#### Listar

```batch
docker image ls
```

#### Baixar

```batch
docker pull <image>
```

#### Remover

```batch
docker rmi <image>
```